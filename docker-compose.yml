services:
  web:
    image: ${IMAGE_NAME}:latest
    container_name: anchorline-web
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/app/data
      - ./uploads:/app/static/uploads
    ports:
      - "5000:5000"